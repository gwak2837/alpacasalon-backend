type Notification {
  id: UUID!
  creationTime: DateTime!
  type: NotificationType!
  contents: NonEmptyString!

  # from other table
  receiver: User!
  sender: User
}

enum NotificationType {
  NEW_COMMENT
  LIKING_COMMENT
  NEW_SUBCOMMENT
}
