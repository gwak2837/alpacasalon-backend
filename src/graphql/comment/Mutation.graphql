type Mutation {
  toggleLikingComment(id: ID!): Comment

  createComment(postId: ID!, contents: NonEmptyString!, parentCommentId: ID): Comment

  updateComment(id: ID!, contents: NonEmptyString!): Comment

  deleteComment(id: ID!): Comment
}
